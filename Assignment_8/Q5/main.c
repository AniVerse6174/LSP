#include <stdio.h>
#include "math.h"

int main() {
    int x = 20, y = 5;
    printf("Add: %d\n", Add(x, y));
    printf("Sub: %d\n", Sub(x, y));
    printf("Mul: %d\n", Mul(x, y));
    printf("Div: %d\n", Div(x, y));
    return 0;
}